# WavFlow runtime requirements
# Install in a fresh conda env (Python >=3.10) like:
#   conda create -n wavflow python=3.10 -y && conda activate wavflow
#   # Step 1: install PyTorch matching your CUDA version. See
#   #   https://pytorch.org/get-started/locally/
#   # E.g. for CUDA 12.6:
#   #   pip install --index-url https://download.pytorch.org/whl/cu126 \
#   #       torch torchaudio torchvision
#   # Step 2:
#   pip install -r requirements.txt

# ---- Core ML ----
# torch / torchaudio / torchvision should be installed first (see header).
# These constraints are kept loose so the CUDA build chosen above wins.
torch>=2.8.0
torchaudio
torchvision
torchdiffeq>=0.2.5

# ---- Numerics / data ----
numpy>=2.2.6
pandas>=2.3.3
scipy>=1.15.3
einops>=0.8.2

# ---- Models / encoders ----
timm>=1.0.27
open_clip_torch>=3.3.0

# ---- Audio ----
pyloudnorm>=0.2.0

# ---- Config / utilities ----
omegaconf>=2.3.0
tqdm>=4.67.3
pillow>=12.2.0
PyYAML>=6.0.3
requests>=2.33.1

# ---- HuggingFace hub (for pretrained weights) ----
huggingface_hub>=1.14.0
safetensors>=0.7.0
